# https://moonrepo.dev/docs/config/toolchain
$schema: 'https://moonrepo.dev/schemas/toolchain.json'

####################################################################################
# NOTE:
# If you created this repository based on the template at https://github.com/svallory/moonrepo-base
# You can should pick one of the options below
####################################################################################

# Extend the template configuration and delete the local `toolchain.base.yml` to avoid confusion
extends: 'https://raw.githubusercontent.com/svallory/moonrepo-template/master/.moon/toolchain.base.yml'

# Part ways with the template and use the local file
# extends: './toolchain.base.yml'